/**
 * INTERFACE FOR THE BRAND LOGO IN THE HEADER
 */
interface LogoInterface {
  /**
   * REQUIRED - name from file
   */
  logo: string;
  /**
   * REQUIRED -  number in pixels for icon width
   */
  width: number;
  /**
   * REQUIRED -  number in pixels for icon height
   */
  height: number;
}
